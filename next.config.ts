/** @type {import('next').NextConfig} */

const nextConfig = {
  output: 'export',
  reactStrictMode: true,
  basePath: '/AlligatorChess',
  images: {
    unoptimized: true
  },
};

// module.exports = nextConfig
export default nextConfig;
